# Minimal Skill Example

This is a documentation-only example of a minimal ASI-compatible skill.

It demonstrates:

- Basic manifest structure
- Required `files` inventory
- Placement of `asi/signature.json`

This example is not automatically signed.
